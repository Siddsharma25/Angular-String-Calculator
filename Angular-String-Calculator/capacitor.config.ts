import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Angular-String-Calculator',
  webDir: 'www'
};

export default config;
